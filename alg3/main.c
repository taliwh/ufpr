/* trabalho alg 3 thalisia grr20256088 arvore bpapapap */

#include <stdio.h>

int
main()
{
  // int32_t t;

  s // canf("%d", t);
      /*
      if (t > 1073741823 || t < 2)
        {
          fprint(stderr, "Valor de t invalido.");
          exit(1);
        }
      */

      // testes
      return 0;
}